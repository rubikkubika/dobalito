name: Build and Test

on:
  push:
    branches: [ main ]
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Build Backend Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        file: ./backend/Dockerfile
        push: false
        tags: dobalito-backend:test
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Build Frontend Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./frontend-react
        file: ./docker/Dockerfile.frontend-react
        push: false
        tags: dobalito-frontend:test
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Test Docker Compose
      run: |
        # Проверяем, что docker-compose.yml корректен
        docker-compose -f docker/docker-compose.yml config
        
        # Проверяем, что все сервисы могут быть созданы
        docker-compose -f docker/docker-compose.yml --env-file env.local config
    
    - name: Run basic tests
      run: |
        echo "✅ Backend image built successfully"
        echo "✅ Frontend image built successfully"
        echo "✅ Docker Compose configuration is valid"
        echo "🎉 All builds completed successfully!"
