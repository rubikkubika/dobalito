# Исправление ошибки 403 на главной странице

## 🐛 Проблема

При переходе на главную страницу (http://localhost:3000) появлялась ошибка:

```
GET http://localhost:8080/api/v1/users/profile 403 (Forbidden)
Error fetching data: AxiosError {message: 'Request failed with status code 403'}
```

**Причина:** HomePage пытался загрузить профиль пользователя даже для неавторизованных пользователей.

## ✅ Решение

### Изменения в HomePage.tsx:

1. **Добавлен импорт useAuth:**
```typescript
import { useAuth } from '../context/AuthContext';
```

2. **Добавлена проверка авторизации:**
```typescript
const { isAuthenticated } = useAuth();
```

3. **Обновлена логика загрузки данных:**
```typescript
useEffect(() => {
  const fetchData = async () => {
    dispatch({ type: 'SET_LOADING', payload: true });
    
    try {
      // Загружаем профиль только если пользователь авторизован
      if (isAuthenticated) {
        const userProfile = await apiService.getUserProfile();
        dispatch({ type: 'SET_USER', payload: userProfile });
      }
    } catch (error) {
      console.error('Error fetching data:', error);
      // Не показываем ошибку для неавторизованных пользователей
      if (isAuthenticated) {
        dispatch({ type: 'SET_ERROR', payload: 'Ошибка загрузки данных' });
      }
    } finally {
      dispatch({ type: 'SET_LOADING', payload: false });
    }
  };

  fetchData();
}, [dispatch, isAuthenticated]); // Добавлена зависимость isAuthenticated
```

## 🔧 Логика работы

### Для неавторизованных пользователей:
- ✅ **Не делается запрос** к `/api/v1/users/profile`
- ✅ **Нет ошибок 403** в консоли
- ✅ **Главная страница загружается** без проблем
- ✅ **Показываются категории** и функции приложения

### Для авторизованных пользователей:
- ✅ **Загружается профиль** пользователя
- ✅ **Обновляется состояние** приложения
- ✅ **Показываются персональные данные**

## 🧪 Тестирование

### 1. Неавторизованный пользователь:
```bash
# Открыть http://localhost:3000
# Проверить консоль браузера - НЕТ ошибок 403
# Главная страница загружается корректно
```

### 2. Авторизованный пользователь:
```bash
# Авторизоваться через /login
# Перейти на главную страницу
# Профиль загружается без ошибок
```

### 3. Проверка API:
```bash
# Неавторизованный запрос (должен вернуть 403)
curl -v http://localhost:8080/api/v1/users/profile

# Авторизованный запрос (должен вернуть данные профиля)
curl -H "Authorization: Bearer <token>" http://localhost:8080/api/v1/users/profile
```

## 📊 Результат

### ✅ Исправлено:
- **Нет ошибок 403** при загрузке главной страницы
- **Корректная работа** для неавторизованных пользователей
- **Загрузка профиля** только для авторизованных пользователей
- **Чистая консоль** без лишних ошибок

### 🎯 Поведение:
- **Неавторизованные пользователи** видят общую информацию о приложении
- **Авторизованные пользователи** видят персональные данные
- **Нет лишних запросов** к защищенным API

## 🔄 Дополнительные улучшения

Можно также добавить:

1. **Индикатор загрузки** для авторизованных пользователей
2. **Кэширование профиля** для избежания повторных запросов
3. **Обработку ошибок** с более детальными сообщениями

## 🎉 Готово!

Главная страница теперь работает корректно для всех пользователей без ошибок 403! 🚀
